Optional Elasticsearch + Kibana + parsedmarc stack from
patschi/parsedmarc-dockerized. Self-hosted, flagged as optional (~VPS cost if
you choose to run it). The default operator path is SQLite/DuckDB + Plotly HTML
and does not need this compose file. No Trivy, no Steampipe, no paid SaaS.
