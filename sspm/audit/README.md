# sspm/audit

Workspace + OSS-tool inventory. `sspm audit inventory` writes `sspm-inventory.json` with package, version, license, and last-update. Primary tools: pip-audit and Mend Bolt. No paid scanners.
