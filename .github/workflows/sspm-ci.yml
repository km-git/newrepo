name: sspm-ci

on:
  pull_request:
  push:
    branches: [main]
  merge_group:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"

      - name: Install SSPM
        run: pip install -e ".[dev]"

      - name: Ruff
        run: ruff check sspm tests/test_sspm.py

      - name: Pytest SSPM
        run: pytest tests/test_sspm.py -q --tb=short

      - name: pip-audit --strict
        run: pip-audit --skip-editable --desc on

      - name: make sspm-all
        run: make sspm-all
