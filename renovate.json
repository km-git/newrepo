{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Mend Renovate (free GitHub App / self-host AGPL). Complements Dependabot. Not Mend SCA / Socket / Aikido paid.",
  "extends": ["config:best-practices"],
  "labels": ["dependencies"],
  "timezone": "Australia/Sydney",
  "schedule": ["before 9am on monday"],
  "prConcurrentLimit": 4,
  "dependencyDashboard": true,
  "packageRules": [
    {
      "description": "SHA-pin GitHub Actions the same way lint-security.yml already does",
      "matchManagers": ["github-actions"],
      "pinDigests": true
    },
    {
      "description": "One weekly Python minor/patch PR instead of a firehose",
      "matchManagers": ["pip_requirements"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "python minor/patch"
    },
    {
      "matchPackageNames": ["torch", "trivy"],
      "enabled": false
    }
  ]
}
