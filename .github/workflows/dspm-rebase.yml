name: dspm-rebase

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  rebase:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    steps:
      # Do not checkout PR head (CodeQL actions/untrusted-checkout). Dependabot
      # can rebase itself when mentioned; no untrusted git + contents:write.
      - name: Ask Dependabot to rebase onto main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: gh pr comment "$PR" --body "@dependabot rebase"
