[project]
name = "sspm"
version = "0.1.0"
description = "SSPM-as-report: read-only SaaS configuration posture reports"
requires-python = ">=3.11"
dependencies = [
  "duckdb==1.5.5",
  "pyarrow==23.0.1",
  "msgraph-sdk==1.5.4",
  "google-api-python-client==2.165.0",
  "PyGithub==2.6.1",
  "slack-sdk==3.36.0",
  "httpx==0.28.1",
  "pydantic>=2.12.5,<3",
  "typer==0.16.0",
  "pyyaml>=6.0.3,<7",
  "feedparser==6.0.11",
  "rich==14.0.0",
  "jinja2==3.1.6",
  "psycopg[binary]==3.2.5",
  "croniter==6.0.0",
  "fastapi==0.141.1",
  "uvicorn[standard]==0.34.0",
]

[project.optional-dependencies]
pii = [
  "presidio-analyzer==2.2.364",
  "presidio-anonymizer==2.2.364",
]
dev = [
  "pytest==9.0.3",
  "pytest-cov==6.0.0",
  "ruff==0.9.4",
  "mypy==1.14.0",
  "pip-audit==2.9.0",
  "detect-secrets==1.5.0",
]

[project.scripts]
sspm = "sspm.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sspm"]

[tool.pytest.ini_options]
testpaths = ["tests", "sspm"]
pythonpath = ["."]
