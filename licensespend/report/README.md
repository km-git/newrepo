# Report

The product: static HTML + Markdown + JSON reclaim pack plus LicenseSpend Explorer (`licensespend ui --static` → `reports/licensespend_explorer.html`). SHA-256 watermark of canonical JSON. Free pack lists hashed user ids + SKU, never email. Set `LICENSESPEND_INCLUDE_EMAIL=1` for a local Pro pack. Draft for human review — not an auto-revoker. Explorer lives in this module, not a ninth first-class module.
