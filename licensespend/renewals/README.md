# Renewals

Reads `licensespend/renewals/contracts.yaml` (vendor, seats, renew_on, notice_days, amount_aud). `licensespend renewals upcoming --days 60` lists contracts in the window. Optional `--ics` export. Nudge text is a draft for a human, not an email send.
