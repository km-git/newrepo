# Slack seats

Parses `examples/slack/users_list.json` (`users.list` + `team.info` shape). Primary live extra: `slack-sdk`. Never calls `conversations.history`. Guests are labelled, not billed. Honest gap: Enterprise Grid org billing may need admin APIs; fixtures cover it.
