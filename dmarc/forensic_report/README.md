Forensic (RUF) parser. Many senders skip RUF for privacy; empty output is a
valid observation, not a failure. From: addresses are masked (Presidio when
installed from data-privacy-stack/presidio; regex otherwise). CLI:
`dmarc forensic list --since 30d`.
