# Loop

Reuses `forum-watcher/scripts/watch.py`. Extra sources live in `forum-watcher/sources.yaml` and this module's `sources.yaml`. Discover threshold is 7. Skip Zylo/Productiv/Torii. Classify unused seats with hashed user ids only. Monthly rollup writes `monthly/license-spend.md`.
