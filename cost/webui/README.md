# cost/webui

Stdlib HTTP dashboard (no Flask/FastAPI) for the Cost & Configuration Review.
Routes: `/cost`, `/api/cost/status`, `/api/cost/report`. `--static` writes
`reports/cost_explorer.html` for Cursor Cloud (the VM bind is not the laptop).
