# Report

The product: static HTML + Markdown + JSON reclaim pack. SHA-256 watermark of canonical JSON. Free pack lists hashed user ids + SKU, never email. Set `LICENSESPEND_INCLUDE_EMAIL=1` for a local Pro pack. Draft for human review — not an auto-revoker.
