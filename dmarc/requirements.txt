# Lightweight deps so `pytest tests/` can run the deliverability toolkit.
# Heavy extras (parsedmarc, Presidio, DuckDB, psycopg) stay in dmarc/pyproject.toml.
dnspython>=2.7.0
jinja2>=3.1.5
cryptography>=42.0.0
