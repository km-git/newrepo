# Audit

`licensespend audit inventory` lists OSS tools, versions, licenses, and a pip-audit/OSV.dev scan when `pip-audit` is on PATH. Metadata only — no tenant secrets. Trivy must be 0.71.2 (never 0.69.4).
