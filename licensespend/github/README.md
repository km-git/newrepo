# GitHub seats

Parses `examples/github/members.json` plus `audit_log.json` for last-active. Live extra: PyGithub (LGPL-3.0) stays a pip dependency — do not copy source into this repo. Never clone private repos. Contribution stats without the audit log are incomplete.
