name: cost-ci

on:
  pull_request:
  push:
    branches: [main]
  merge_group:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"

      - name: Install COST (stdlib extras only)
        run: pip install -e ".[dev]"

      - name: Ruff
        run: ruff check --config ruff.toml cost tests/test_cost_architecture.py tests/test_cost_core.py tests/test_cost_loop.py tests/test_cost_webui.py

      - name: Mypy
        run: mypy cost/

      - name: Pytest COST
        run: pytest tests/test_cost_architecture.py tests/test_cost_core.py tests/test_cost_loop.py tests/test_cost_webui.py -q --tb=short

      # Advisory only: --strict fails because unpublished local cost is not on PyPI,
      # and falling back to EW requirements.txt hits diskcache PYSEC-2026-2447 (no fix).
      # continue-on-error so Trivy still runs.
      - name: pip-audit (advisory)
        continue-on-error: true
        run: pip-audit --skip-editable --desc on

      - name: Setup Trivy (safe post-CVE-2026-33634 pin)
        uses: aquasecurity/setup-trivy@3fb12ec12f41e471780db15c232d5dd185dcb514 # v0.2.6
        with:
          version: v0.71.2

      - name: Refuse malicious Trivy window
        run: |
          ver=$(trivy --version)
          echo "$ver"
          python -c "from cost.tools import assert_trivy_allowed; import subprocess; t=subprocess.check_output(['trivy','--version'], text=True); print(assert_trivy_allowed(t))"

      - name: Trivy filesystem scan
        run: trivy fs --severity HIGH,CRITICAL --exit-code 1 --config trivy.yaml .
